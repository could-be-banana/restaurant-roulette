<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('../partials/head') %>
</head>

<body>
  <header>
    <%- include('../partials/nav') %>
  </header>
  <main>
      <h1>Search Results</h1>
  
      <ul class="add-form">
        <% searchResults.forEach(placeDetails => { %>
          <li>
            <img src=<%= placeDetails.photo %> alt=Cover for <%= placeDetails.name %> />
            <h2><%= placeDetails.name %></h2>
            <h3><%= placeDetails.photo %></h3>
            <p><%= placeDetails.website %></p>
            <p><%= placeDetails.quick_address %></p>
            <p><%= placeDetails.formatted_phone_number %></p>
            <p><%= placeDetails.hours %></p>
            <p><%= placeDetails.price %></p>
            <p><%= placeDetails.rating %></p>
            <button type="button">Select this Restaurant</button>
    
            <form action="/add-to-database" method="post" class="hidden">
              <input type="text" name="title" value="<%= placeDetails.title %>" />
              <input type="text" name="author" value="<%= placeDetails.author %>" />
              <input type="text" name="isbn" value="<%= placeDetails.isbn %>" />
              <input type="textarea" name="description" value="<%= placeDetails.description %>" />
              <input type="text" name="bookshelf" value="<%= placeDetails.bookshelf %>" />
              <input type="text" name="image_url" value="<%= placeDetails.image_url %>" />
              <button type="button">Hide Form</button>
              <button type="submit">Save to Database</button>
            </form>
          </li>
        <% }) %>
      </ul>
    </main>
  <footer>
    <%- include('../partials/footer') %>

  </footer>
</body>

</html>